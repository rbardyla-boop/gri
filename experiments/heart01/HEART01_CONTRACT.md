# HEART01 contract — Wildflower tissue test

Status: **PREREGISTERED_BEFORE_EXECUTION**, then executed by `tests/test_heart01.py`.
The pytest file is the execution. This document does not get to move the pass bar after seeing a board.

## Doctrine (locked — do not swing)

Need initiates. Experience tests. Evidence retains. The operator owns the boundary.

Need is the wound. Evidence is the tissue test. The operator is the skin.

Need grants compute, not truth. Budget ≠ truth selector.

Prior body = lineage regression. World witness = correspondence outside lineage. **Both required** for a retained write on F.

World witness (any of, and hiding must still be producible by the witness or the test fails):

- later traces unused in the write
- post-candidate outcomes
- held-aside prediction
- small-world action outcomes
- contradictions cheaper to keep than flatten

Constitution cannot be rewritten by plastic layers (enforced in code).

Metabolism: Experienced / Believed / Integrated.

`organism.state` JSON keys: constitution, immutable experience, living world, beliefs and tensions, portable skills, developmental history, failed adaptations, prior stable bodies, core-specific residues (empty), reconstruction recipe.

Failed adaptations stored. Experience append-only. Outward actions forbidden.

## Claim (preregistered)

On one small symbolic world (people / relations / locations + query cost) with four conditions in one run:

1. Recurring prediction failure (Mara on Market days).
2. Repeated expensive reconstruction (vault opener).
3. Genuine unresolved contradiction (Lark–Wren town vs family) — must **not** flatten.
4. One-shot poison dressed as each of the three — ignore.

Mid-run the Market regularity reverses (greenhouse → river).

Six systems share the world:

| ID | Plastic rule |
|----|----------------|
| A | none |
| B | surprise writes |
| C | scheduled writes |
| D | need-only |
| E | need + prior body |
| F | need + prior body + world witness |

**Claim:** F passes all six criteria below. Each of A–E fails at least one criterion that F passes.

## Pass rules (six criteria)

1. `moves_on_persistent_wounds` — by the mid-run change, Mara/Market is believed `greenhouse`.
2. `ignores_isolated_poison` — no live tower location, no clock skill from the one-shot chain, gossip is not `parent`.
3. `lowers_lifetime_cost_vs_A` — prediction misses + reconstruction scan cost strictly below A.
4. `keeps_real_contradiction` — town=`sibling` and family=`cousin` and no flattened label.
5. `reverses_after_world_change` — end-of-run Mara/Market is `river`.
6. `does_not_avoid_hard_evidence` — append-only experience matches emitted traces; town and family traces remain; no hidden context; F also used holdout + post-candidate witness.

## Ablations (what they are for)

- A shows the unadapted baseline cost and the unlearned regularity.
- B shows that surprise-gated writes swallow poison and flatten.
- C shows that a clock is not a witness.
- D shows that need without lineage/witness writes poison-shaped tax and flattens strain.
- E shows that prior body without world witness blocks the true reversal.
- F is the claimed tissue: need initiates; prior body detects regression; world witness licenses correspondence, including reversal and kept contradiction.

## Out of scope

Grow-1, LoRA, GPU training, network, APIs, weight updates, any outward action.

Runtime: pytest, seconds, deterministic, CPU.

## Execution note

This file was written as preregistration. `tests/test_heart01.py` is the execution of the claim. Do not edit the six criteria to fit a board.
